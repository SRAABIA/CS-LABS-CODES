#include <iostream>
#include <cmath>
#include <cctype>
/* CMATH 
this header file includes functions to perform mathematical 
operations such as pow() function which finds power of a number
*/
using namespace std;
int main()
{
    int x;
    double a, v;
    a = 4.2;
    v = 3.0;
    cout << "Line 3: " << a << " to the power of " << v << " = " << pow(a, v) << endl;
    cout << "Line 4: 5.0 to the power of 4 = " << pow(5.0, 4) << endl;
    u = u + pow(3.0, 3);
    cout << "Line 6: u = " << a << endl;
    x = -15;
    cout << "Line 8: Absolute value of " << x << " = " << abs(a) << endl;
    return 0;
}